{:port 3000
 :database-url "guestbook.sqlite3"
 :name "Jolt"
 :motd "ring-core + hiccup + config, all from deps.edn, on jolt's native HTTP server + SQLite"
 :features ["ring middleware (params, keyword-params)"
            "hiccup HTML"
            "yogthos/config (PORT env overrides :port)"
            "jolt's built-in HTTP server (BSD sockets via FFI)"
            "a SQLite guestbook (jolt's built-in libsqlite3 binding)"]}
