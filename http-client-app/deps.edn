{:paths ["src"]

 ;; The HTTP client is built into jolt (jolt.http-client, over the system curl);
 ;; this example has no external dependencies.
 :deps {}

 :tasks {run {:doc "hit some real HTTPS endpoints" :main-opts ["-m" "app.core"]}}}
